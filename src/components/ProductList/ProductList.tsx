import React from "react";
import { MainReduserState } from "../../interfaces/interfaces";

type Props = Partial<MainReduserState>

const ProductList = (props: Props) => {
    return (
        <div>
            Product List
        </div>
    )
}

export default ProductList